# 00. RAG란 무엇인가?

## 도서관에서 시험 보기

RAG를 이해하기 위해, 시험을 보는 학생을 상상해봅시다.

### 시나리오 1: 오픈북 없이 시험

학생이 교실에 앉아 있습니다. 시험지를 받았는데, 수업 시간에 배운 내용만으로 답해야 합니다.

- 수업에서 다룬 내용 → 잘 답변합니다
- 수업에서 다루지 않은 내용 → 모르지만, **그럴듯하게 지어냅니다**
- 수업 이후에 바뀐 내용 → 예전 정보로 답변합니다

이것이 바로 **일반 LLM**의 동작 방식입니다.

### 시나리오 2: 도서관에서 오픈북 시험

이번에는 다릅니다. 시험 전에 **도서관 사서**가 도와줍니다.

1. 학생이 문제를 읽습니다: "양자 컴퓨팅의 최신 동향은?"
2. 사서가 관련 책을 찾아서 **해당 페이지만** 펼쳐줍니다
3. 학생은 그 페이지를 읽고 답변합니다
4. 답변에 "이 내용은 《양자 컴퓨팅 2025》 p.42에서 참고했습니다"라고 적습니다

이것이 **RAG**입니다.

```
┌─────────────────────────────────────────────┐
│              일반 LLM (시나리오 1)             │
│                                             │
│   질문 ──→ [머릿속 기억만으로] ──→ 답변        │
│            (틀릴 수도 있음)                    │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│              RAG (시나리오 2)                 │
│                                             │
│   질문 ──→ [관련 문서 찾기] ──→ [문서를 읽고  │
│              (사서 역할)        답변 작성]     │
│                                ──→ 답변      │
│                           (출처 포함)         │
└─────────────────────────────────────────────┘
```

## RAG = Retrieval-Augmented Generation

한국어로 **검색 증강 생성**이라고 합니다. 이름을 하나씩 뜯어보면:

| 영어 | 한국어 | 의미 |
|------|--------|------|
| **Retrieval** | 검색 | 관련 문서를 찾는다 |
| **Augmented** | 증강 | LLM의 능력을 보강한다 |
| **Generation** | 생성 | 답변을 만들어낸다 |

즉, "관련 문서를 **검색**해서 LLM의 능력을 **증강**하여 더 나은 답변을 **생성**한다"는 뜻입니다.

## LLM의 세 가지 한계

RAG가 왜 필요한지 이해하려면, LLM의 한계를 알아야 합니다.

### 한계 1: 지식 컷오프 (Knowledge Cutoff)

LLM은 학습이 끝난 시점까지의 정보만 알고 있습니다.

```
예시:
  질문: "2026년 1월에 출시된 새로운 MLX 기능은?"
  LLM:  "제가 알고 있는 정보로는..." (학습 데이터에 없으므로 답변 불가)
  RAG:  최신 릴리스 노트를 검색 → 정확한 답변
```

### 한계 2: 환각 (Hallucination)

LLM은 모르는 것도 그럴듯하게 지어냅니다. 자신감 있게 틀린 답을 합니다.

```
예시:
  질문: "우리 회사의 휴가 정책은?"
  LLM:  "일반적으로 연 15일의 유급 휴가가..." (지어낸 답변!)
  RAG:  사내 규정 문서를 검색 → 실제 정책으로 답변
```

### 한계 3: 개인/내부 데이터에 접근 불가

LLM은 인터넷의 공개 데이터로 학습되었습니다. 여러분의 개인 문서, 회사 내부 문서, 로컬 파일의 내용은 전혀 모릅니다.

```
예시:
  질문: "내 프로젝트의 API 엔드포인트 목록은?"
  LLM:  "일반적인 REST API 엔드포인트는..." (일반론만 답변)
  RAG:  프로젝트 문서를 검색 → 실제 엔드포인트 목록 반환
```

## RAG가 해결하는 방법

RAG는 LLM에게 **참고 자료를 건네주는** 것입니다.

시험에 비유하면:
- 파인튜닝 = 시험 전에 새로운 내용을 더 공부시키는 것 (비싸고 오래 걸림)
- RAG = 시험 중에 참고 자료를 펼쳐주는 것 (빠르고 저렴)

| | RAG | 파인튜닝 |
|---|---|---|
| 새 정보 추가 | 문서만 넣으면 끝 | 모델을 다시 학습해야 함 |
| 비용 | 저렴 (임베딩 계산만) | 비쌈 (GPU 학습 필요) |
| 시간 | 분 단위 | 시간~일 단위 |
| 출처 추적 | 가능 ("이 문서에서 왔습니다") | 불가능 |
| 정보 업데이트 | 문서 교체만 하면 됨 | 재학습 필요 |

## 실제로 어디에 쓰이나?

RAG는 이미 많은 곳에서 사용되고 있습니다:

- **사내 검색 챗봇** — 회사 위키, 규정, 매뉴얼을 검색해서 답변
- **고객 지원** — FAQ, 제품 문서를 기반으로 답변
- **코드 어시스턴트** — 프로젝트 코드를 검색해서 코딩 질문에 답변
- **논문 분석** — 연구 논문을 넣고 내용에 대해 질문
- **개인 지식 관리** — 내 노트, 메모를 검색해서 필요한 정보를 찾기

## 이 프로젝트에서 만드는 것

우리는 **Mac Studio에서 완전히 로컬로 돌아가는 RAG 시스템**을 만듭니다.

- 인터넷 연결 없이 동작합니다
- 내 문서(Markdown, PDF, 텍스트 파일)를 넣으면 검색합니다
- Qwen 모델이 검색 결과를 바탕으로 답변합니다
- 답변에 "이 정보는 어떤 파일에서 왔는지" 출처를 표시합니다

그리고 가장 중요한 것:

**같은 질문을 RAG 없이 / RAG와 함께 물어보고, 답변 품질이 어떻게 달라지는지 직접 비교합니다.**

## 다음 단계

다음 튜토리얼에서는 RAG의 전체 과정을 큰 그림으로 살펴봅니다. 각 단계가 무엇을 하는지 한눈에 이해할 수 있게 됩니다.

---

다음: [01. RAG의 전체 그림](01-rag-big-picture.md)
