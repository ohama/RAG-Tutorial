# 01. RAG의 전체 그림

## 두 개의 파이프라인

RAG는 크게 두 단계로 나뉩니다. **준비 단계**와 **질문 단계**입니다.

다시 도서관 비유로 설명하면:

1. **준비 단계 (Ingestion)** = 사서가 책을 분류하고 서가에 정리하는 과정
2. **질문 단계 (Query)** = 학생이 질문하면 사서가 관련 책을 찾아주는 과정

준비 단계는 **한 번만** 하면 됩니다. 새 책(문서)이 추가될 때만 다시 합니다.
질문 단계는 **매 질문마다** 반복됩니다.

## 준비 단계 (Ingestion Pipeline)

문서를 검색 가능한 형태로 변환하는 과정입니다.

```
 ① 로딩        ② 청킹          ③ 임베딩         ④ 저장
┌──────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ 문서  │ →  │ 텍스트    │ →  │ 텍스트    │ →  │ 숫자 벡터 │ →  벡터 DB
│ 파일  │    │ 추출     │    │ 조각들    │    │ 변환     │    (ChromaDB)
│.md    │    │          │    │          │    │          │
│.pdf   │    │ "안녕    │    │ [조각1]  │    │ [0.1,    │
│.txt   │    │  하세요  │    │ [조각2]  │    │  0.3,    │
│       │    │  ..."    │    │ [조각3]  │    │  ...]    │
└──────┘    └──────────┘    └──────────┘    └──────────┘
```

각 단계를 하나씩 살펴봅시다:

### ① 로딩 (Document Loading)

파일을 열어서 텍스트를 추출합니다.

- `.txt` 파일 → 그대로 읽기
- `.md` 파일 → 마크다운 문법과 함께 읽기
- `.pdf` 파일 → PDF 레이아웃을 텍스트로 변환 (가장 복잡)

**비유**: 도서관에 새 책이 들어왔습니다. 사서가 포장을 뜯고 책을 펼칩니다.

### ② 청킹 (Chunking)

긴 텍스트를 적당한 크기의 **조각(chunk)**으로 나눕니다.

왜 나누는가?
- 책 한 권을 통째로 학생에게 주면 읽는 데 오래 걸립니다
- 필요한 **페이지만** 펼쳐주는 것이 훨씬 효율적입니다
- LLM도 한 번에 처리할 수 있는 텍스트 양에 한계가 있습니다

```
원본 문서 (3000자):
"RAG는 검색 증강 생성입니다. LLM이 답변을 생성할 때...
 ...임베딩은 텍스트를 벡터로 변환합니다...
 ...ChromaDB는 벡터를 저장합니다..."

         ↓ 500자씩 나누기

조각 1 (500자): "RAG는 검색 증강 생성입니다. LLM이..."
조각 2 (500자): "...임베딩은 텍스트를 벡터로 변환..."
조각 3 (500자): "...ChromaDB는 벡터를 저장합니다..."
```

**비유**: 사서가 책의 각 장(chapter)을 색인 카드에 요약해 둡니다. 나중에 관련 장만 빠르게 찾을 수 있도록.

### ③ 임베딩 (Embedding)

텍스트 조각을 **숫자 벡터**로 변환합니다. 이것이 RAG에서 가장 핵심적인 개념입니다.

벡터란 무엇인가? 쉽게 말하면 **텍스트의 의미를 숫자로 표현한 것**입니다.

```
"고양이가 소파에 앉아 있다" → [0.82, -0.15, 0.43, ..., 0.67]
"강아지가 침대에 누워 있다" → [0.79, -0.18, 0.41, ..., 0.64]  ← 비슷한 숫자!
"오늘 주식 시장이 폭락했다" → [-0.23, 0.91, -0.56, ..., 0.12] ← 완전 다른 숫자
```

의미가 비슷한 텍스트는 비슷한 벡터를 가집니다. 이 성질 덕분에 "의미로 검색"이 가능합니다.

**비유**: 사서가 각 색인 카드에 "이 장의 내용을 좌표로 표현하면 (X, Y, Z)입니다"라고 적어둡니다. 비슷한 내용의 카드는 비슷한 좌표를 가집니다.

### ④ 저장 (Vector Store)

벡터를 데이터베이스에 저장합니다. 나중에 질문이 들어오면 빠르게 비슷한 벡터를 찾을 수 있도록 합니다.

이 프로젝트에서는 **ChromaDB**를 사용합니다. 설치만 하면 바로 쓸 수 있는 가벼운 벡터 데이터베이스입니다.

**비유**: 사서가 색인 카드를 서가에 정리해 둡니다. 좌표가 비슷한 카드끼리 가까운 서랍에 넣습니다.

## 질문 단계 (Query Pipeline)

사용자가 질문하면 관련 문서를 찾아서 답변을 생성하는 과정입니다.

```
                    ⑤ 질문           ⑥ 검색          ⑦ 프롬프트        ⑧ 생성
                    임베딩                           구성
                 ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
 사용자 질문  →   │ 질문을    │ →  │ 비슷한   │ →  │ 질문 +   │ →  │ LLM이    │ → 답변
 "MLX란?"       │ 벡터로    │    │ 벡터 찾기 │    │ 관련 문서 │    │ 답변     │   (출처
                │ 변환     │    │ (벡터 DB) │    │ 합치기   │    │ 생성     │   포함)
                └──────────┘    └──────────┘    └──────────┘    └──────────┘
```

### ⑤ 질문 임베딩

사용자의 질문도 같은 방법으로 벡터로 변환합니다.

```
"MLX의 장점은 무엇인가?" → [0.45, -0.12, 0.78, ..., 0.33]
```

**중요**: 문서를 임베딩할 때 쓴 것과 **같은 모델**로 질문을 임베딩해야 합니다. 다른 모델을 쓰면 벡터 공간이 달라서 검색이 안 됩니다.

### ⑥ 유사도 검색 (Retrieval)

질문 벡터와 가장 비슷한 문서 조각 벡터를 찾습니다.

```
질문 벡터: [0.45, -0.12, 0.78, ...]

저장된 벡터들:
  조각 1: [0.44, -0.11, 0.77, ...] → 유사도 0.98 ★ 가장 비슷!
  조각 2: [0.43, -0.13, 0.76, ...] → 유사도 0.95 ★ 두 번째
  조각 3: [-0.23, 0.91, -0.56, ...] → 유사도 0.12   관련 없음
  ...

→ 상위 3개 반환: [조각 1, 조각 2, ...]
```

**비유**: 학생이 "MLX의 장점은?"이라고 물었습니다. 사서가 서가에서 비슷한 좌표의 카드를 찾아 관련 페이지를 꺼내줍니다.

### ⑦ 프롬프트 구성 (Prompt Building)

검색된 문서 조각과 질문을 합쳐서 LLM에게 보낼 프롬프트를 만듭니다.

```
┌─────────────────────────────────────────────┐
│ 다음 문서를 참고하여 질문에 답변하세요.         │
│                                             │
│ [참고 문서]                                  │
│ "MLX는 Apple이 만든 ML 프레임워크로,          │
│  통합 메모리를 활용하여..."                    │
│                                             │
│ "Apple Silicon의 통합 메모리 덕분에            │
│  CPU와 GPU가 같은 메모리를..."                │
│                                             │
│ [질문]                                      │
│ MLX의 장점은 무엇인가?                        │
│                                             │
│ [답변]                                      │
└─────────────────────────────────────────────┘
```

이것이 RAG의 핵심입니다. LLM에게 **"이 문서를 읽고 답변해"**라고 하는 것이죠.

### ⑧ 생성 (Generation)

LLM이 주어진 문서와 질문을 읽고 답변을 만듭니다.

```
LLM 답변:
"MLX의 주요 장점은 다음과 같습니다:
 1. Apple Silicon의 통합 메모리를 활용하여 데이터 복사 없이 CPU와 GPU가
    같은 메모리에 접근할 수 있습니다.
 2. ...

 (출처: mlx-guide.md, 조각 3)"
```

## 전체 흐름 한눈에

```
═══════════════════════════════════════════════════
 준비 단계 (한 번만 실행)
═══════════════════════════════════════════════════

    내 문서들                    벡터 DB
  ┌────────┐                  ┌────────┐
  │guide.md│ → 로딩 → 청킹 → │        │
  │paper.pdf│   임베딩 →     │ChromaDB│
  │notes.txt│                │        │
  └────────┘                  └────────┘

═══════════════════════════════════════════════════
 질문 단계 (매번 실행)
═══════════════════════════════════════════════════

  사용자 질문                 벡터 DB        LLM (Qwen)
  "MLX란?"  → 임베딩 → 검색 → 관련 조각 → 프롬프트 → 답변
                      ↑                  구성
                      │
                 같은 벡터 공간에서 비교
```

## 각 단계를 배우는 순서

이 튜토리얼은 위 파이프라인의 각 단계를 하나씩 구현해나갑니다:

| 튜토리얼 | 배우는 단계 | 비유 |
|----------|-----------|------|
| [02. 환경 설정](02-environment-setup.md) | 도구 준비 | 도서관 건물 짓기 |
| [03. LLM 만나기](03-meet-your-llm.md) | ⑧ 생성 | 학생 만나기 |
| [04. 문서 준비](04-document-loading.md) | ① 로딩 | 책 입고 |
| [05. 텍스트 자르기](05-chunking.md) | ② 청킹 | 색인 카드 만들기 |
| [06. 의미를 숫자로](06-embedding.md) | ③ 임베딩 | 좌표 매기기 |
| [07. 벡터 저장소](07-vector-store.md) | ④ 저장 | 서가에 정리 |
| [08. 검색하기](08-retrieval.md) | ⑤⑥ 검색 | 사서가 찾아주기 |
| [09. 파이프라인 조립](09-rag-pipeline.md) | ⑦ 프롬프트 구성 | 전체 연결 |
| [10. RAG의 차이](10-rag-comparison.md) | 최종 비교 | 시험 결과 비교 |

> **팁**: 먼저 LLM을 만나봅니다(03). LLM이 뭘 잘하고 뭘 못하는지 체감한 후에, 나머지 단계의 "왜"가 자연스럽게 이해됩니다.

## 핵심 정리

1. RAG는 **검색 + 생성**입니다
2. **준비 단계**: 문서 → 조각으로 나누기 → 벡터로 변환 → 저장
3. **질문 단계**: 질문 → 관련 조각 검색 → 질문 + 조각을 LLM에 전달 → 답변
4. 모든 것이 **내 컴퓨터에서 로컬로** 동작합니다
5. 프레임워크 없이 각 단계를 직접 만들면, RAG가 어떻게 작동하는지 완전히 이해할 수 있습니다

---

이전: [00. RAG란 무엇인가](00-what-is-rag.md) | 다음: [02. 환경 설정](02-environment-setup.md)
